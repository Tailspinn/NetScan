from distutils.core import setup
setup(name='NetworkScan',
      version='1.1',
      py_modules=['networkscan'],
      long_description="Cross platform threaded tcp network port scanner with module support.",
      )
